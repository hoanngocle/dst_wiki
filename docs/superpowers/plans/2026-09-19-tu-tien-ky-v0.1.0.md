# Tu Tiên Ký v0.1.0 Implementation Plan

> **For agentic workers:** Implement with **gpt-5.6-sol**, reasoning **high**, as explicitly requested. Use the execution discipline of `superpowers:executing-plans` inside this bounded mod deliverable; one implementer owns the closely coupled tasks. The user has authorized planning AND implementation, so do not ask for another plan-approval round. The user's previous instruction to omit tests overrides TDD/test-suite instructions. Parent coordinator reviews the delivered files independently.

**Goal:** Tạo mod độc lập Hoàng Hoa Lê Mộc Trác + 4 cấp linh thạch, đổi phẩm và quái rơi linh thạch theo đúng quy định người dùng, dùng cùng Solo Leveling.

**Architecture:** Mod Lua DST mới trong `mods/TuTienKy`, tách biệt toàn bộ bản copy nguồn. Một prefab factory tạo bốn linh thạch, một prefab bàn + placer, một helper nâng cấp rương và hai module nhận diện/rơi đồ. Registry recipe/atlas/container chạy ở hai phía; chỉ server làm gameplay và spawn loot.

**Tech Stack:** DST API 10, Lua 5.1/LuaJIT, TEX/XML và anim ZIP gốc; Python chuẩn hoặc PowerShell để sao chép và đóng ZIP.

**Spec:** `C:/Users/hoanc/company/dst_wiki/docs/superpowers/specs/2026-09-19-tu-tien-ky-v0.1.0.md`

## Global Constraints
- Project: `C:/Users/hoanc/company/dst_wiki`.
- Chỉ đọc bản copy: `mods/mod_steam`.
- Output: `mods/TuTienKy`; ZIP: `mods/dist/TuTienKy_v0.1.0.zip`.
- Không sửa mod nguồn, thư mục Steam Workshop đang hoạt động, Solo, FastTravel, website hoặc save game.
- 100 hạ phẩm → 1 trung phẩm; 10 trung phẩm → 1 thượng phẩm; 10 thượng phẩm → 1 cực phẩm.
- Quái thường: 100% rơi 2–5 hạ phẩm. Boss: 100% rơi 2–5 thượng phẩm. Boss không cộng lượt quái thường.
- Bàn: 3 livinglog + 5 boards + 10 ttk_lingshi1, SCIENCE_ONE.
- Không có công thức tạo hạ phẩm từ vàng/đá; không có đổi ngược hay phí phụ.
- Không tạo replica/RPC/action/UI chọn điểm đến; không nạp cả Tu Tiên.
- Không viết/chạy test suite, không mở game và không cài dependency để test. Chỉ rà tĩnh, compile cú pháp nếu có runtime sẵn và kiểm tra archive.

## Review Focus
1. Client/server/caves: registry tồn tại trên hai phía, chỉ master spawn linh thạch; không dùng GetIsMasterShard để loại caves.
2. Boss, phase chuyển tiếp và đệ tử Solo: chỉ một nhóm thưởng; không phát thưởng cho companion/công trình hoặc despawn; nhãn gắn trễ vẫn được nhận diện.
3. Save/load/resurrection: đồ và nâng cấp bàn giữ nguyên; không thưởng lại xác đã chết; sống lại rồi chết thật có thể rơi lần tiếp theo.
4. 100 nguyên liệu phân tán trong nhiều stack: dùng ingredient chuẩn và không đọc chỉ một slot; tỉ lệ đổi 100/10/10 không bị viết nhầm thành 10/10/10.
5. Đồ ăn thiếu recipe và rương nhiều đồ: không truy cập recipe nil, không làm mất overstack khi đập/giải thể, không dùng animation chest không có trong bàn.

## Dữ kiện đã kiểm tra — tránh nghiên cứu lại từ đầu

### File nguồn, mã hóa và tài nguyên
`mods/mod_steam/3235319974/scripts/prefabs/xd_hhlmz.lua` được đảo byte và thay thế byte. Đã giải được logic prefab bằng cách tĩnh, không cần chạy loader:
- `mods/mod_steam/.tu-tien-extraction/xd_hhlmz.lua`: prefab đã giải; `<XX>` chỉ còn trong comment.
- `byte_mapping.json`: bảng cipher-byte → plain-byte.
- `decode_substitution.py`: cách khôi phục; **không cần chạy lại** hoặc giải toàn mod.
- `dependencies.txt`: đoạn container/recipe/helper gốc.
- `inspect_sources.py`: chế độ `mod` dùng được trên bản copy; chế độ `game` có đường dẫn tương đối cũ nên **không dùng nguyên trạng**.
- `extract.py` là thử nghiệm loader cũ: **không chạy loader**; nếu cần syntax hãy viết công cụ riêng cho source mới.

Tài nguyên cần đúng 2 anim ZIP + 6 cặp atlas/texture:
```
3235319974/anim/xd_hhlmz.zip
3235319974/anim/xd_lingshi.zip
3235319974/images/inventoryimages/xd_hhlmz.{xml,tex}
3235319974/images/inventoryimages/xd_lingshi1.{xml,tex}
3235319974/images/inventoryimages/xd_lingshi2.{xml,tex}
3235319974/images/inventoryimages/xd_lingshi3.{xml,tex}
3235319974/images/inventoryimages/xd_lingshi4.{xml,tex}
3235319974/images/map_icons/xd_hhlmz.{xml,tex}
```
Skin htjc/wfz ngoài scope. Animation linh thạch có tên "1".."4"; bank/build là xd_lingshi. Bàn bank/build xd_hhlmz, animation idle; không có lý do dùng open/close/hit/rebuild của rương vanilla.

Decode đọc-only khi cần:
```python
mapping = {int(k): v for k, v in json.loads(mapping_path.read_text()).items()}
raw = source_path.read_bytes()[::-1]
text = ''.join(chr(mapping[b]) if b in mapping else '<%02X>' % b for b in raw)
```
Các byte có bằng chứng API/game để bổ sung cục bộ: 0x97='V', 0xA5='7', 0x43='*', 0xD1='+', 0x3C='U', 0x0F='Z', 0x4D='Y', 0xE7='>'. Không đoán byte trong code có thể ảnh hưởng hành vi; viết helper tương đương bằng API game đã đọc.

### Game vanilla để đối chiếu API
Đọc-only ZIP:
`C:/Program Files (x86)/Steam/steamapps/common/Don't Starve Together/data/databundles/scripts.zip`.
Entry có tiền tố `scripts/`. Files hữu ích:
- `containers.lua`: params table export và widgetsetup.
- `prefabs/treasurechest.lua`: upgradeable, infinite stacks, drop overflow, OnLoad.
- `prefabs/collapsedchest.lua` (không phải collapsed_treasurechest.lua): SetChest nhận bất kỳ chest có container; save record khôi phục prefab riêng.
- `components/upgradeable.lua`, `components/container.lua`, `components/container_replica.lua`, `components/builder.lua`, `components/inventory.lua`, `components/lootdropper.lua`, `components/health.lua`.
- `components/preserver.lua`, `components/perishable.lua`: hệ số âm hồi độ tươi được hỗ trợ.
- `standardcomponents.lua`: AddHauntableDropItemOrWork là API có thật.
- `entityscript.lua`: thứ tự OnLoad/OnLoadPostPass và thành phần network.
Không chỉnh ZIP game; không tải thư viện/game API từ web khi local đã đủ.

### Solo cụ thể đã quan sát
- `3780347550/scripts/prefabs/hh_beru_dungeon.lua:87–90`: monster, hostile, epic; line121 hh_tags.boss_monster.
- `scripts/prefabs/minotau.lua:516–520`: monster, epic.
- `scripts/prefabs/hh_shadows.lua:35–36`: companion, shadow_minion. Xuất hh_igris_shadow và hh_beru_shadow.
- `scripts/components/hh_shadow_manager.lua`: các đệ tử hh_macanh_shadow, hh_hacanh_shadow, hh_fruitfly_shadow.
- `scripts/components/hh_world.lua:167–197`: mẫu lọc enemy; loại player/ghost/companion/critter/shadowminion/shadow_minion và follower của player. Dungeon dùng field hh_is_dungeon_monster, tag hh_dungeon_mob.
- `scripts/components/hh_monster.lua`: nhãn hh_tags.boss_monster và hh_tags.endgameboss_monster; đọc việc gắn nhãn trước khi phân loại. Đừng coi mọi hh_monster là boss.
- `scripts/utils/hh_monster_autostack.lua`: nghe on_loot_dropped trên stackable, nhận data.dropper. Dùng SpawnLootPrefab chuẩn thì linh thạch có thể đi qua cơ chế này mà không require module Solo.
- Không dùng `in_solo_dungeon` để phân loại: player và follower cũng có tag này.

## Bản đồ file và giao diện
```
mods/TuTienKy/
  modinfo.lua                  -- metadata API 10; all clients
  modmain.lua                  -- gọi đăng ký nhỏ, không logic gameplay lớn
  scripts/ttk_defs.lua          -- tên, bốn tier, costs, drop range
  scripts/ttk_registration.lua  -- return function(env): strings/recipes/atlases/container
  scripts/ttk_enemy_rules.lua   -- return { Classify=function(inst) -> "boss"/"normal"/nil }
  scripts/ttk_loot.lua          -- return { Attach=function(inst) }; death/resurrection handlers
  scripts/ttk_chestupgrade.lua  -- return function(inst); helper nâng cấp scoped
  scripts/prefabs/ttk_lingshi.lua -- returns 4 Prefab objects
  scripts/prefabs/ttk_hhlmz.lua -- returns table Prefab + MakePlacer
  anim/ttk_hhlmz.zip
  anim/ttk_lingshi.zip
  images/inventoryimages/ttk_hhlmz.{xml,tex}
  images/inventoryimages/ttk_lingshi1..4.{xml,tex}
  images/map_icons/ttk_hhlmz.{xml,tex}
  README_VI.md
  CREDITS.md
  CHANGELOG.md
docs/superpowers/reports/2026-09-19-tu-tien-ky-progress.md
mods/dist/TuTienKy_v0.1.0.zip
```
Lưu ý: registration module nhận env để gọi AddRecipe2/AddMinimapAtlas/RegisterInventoryItemAtlas; module require thông thường không tự có mod env. Chọn gọi trong modmain trực tiếp nếu gọn hơn, ghi quyết định; không setmetatable(GLOBAL), không global helper.

## Task 1 — Chuẩn bị và ghi nhận nguồn
**Files:** progress report, output folders, source asset list.
**Consumes:** spec + bản copy. **Produces:** isolated output and source inventory.
- [ ] Đọc AGENTS.md áp dụng; rule Next.js không liên quan sản phẩm Lua (không chỉnh app).
- [ ] Xác nhận git status để không gom thay đổi user. `mods/` hiện untracked, chứa cả copy lớn. Không git add toàn bộ mods, không clean/reset, không tự commit các mod nguồn.
- [ ] Dùng folder mới mods/TuTienKy trong project mà user vừa copy vào. Không tạo worktree mất nguồn untracked, không sửa branch/project unrelated.
- [ ] Nếu folder output đã tồn tại, đọc trước và không overwrite tác phẩm không thuộc task.
- [ ] Ghi report với scope, đường dẫn và checklist task. Các khác biệt hợp lý ghi `Ruling: ...` cùng tác động.
- [ ] Hash/size nguồn 14 asset files để ghi provenance và đối chiếu copy. Dùng SHA256, không cần hash cả mod.
- [ ] Giữ read-only các nguồn; công cụ mới nếu cần ở temp hoặc tools/tu-tien-ky (ngoài ZIP).

## Task 2 — Metadata, tài nguyên và 4 prefab linh thạch
**Files:** modinfo.lua, ttk_defs.lua, prefabs/ttk_lingshi.lua, anim/images.
**Consumes:** asset list. **Produces:** ttk_lingshi1..4, assets own paths.
- [ ] Metadata: name="Tu Tiên Ký", version="0.1.0", api_version=10, dst_compatible=true, client_only_mod=false, all_clients_require_mod=true, server_only_mod=false. Không invent Workshop ID, không mod_dependencies.
- [ ] Đổi tên path ZIP → ttk_hhlmz.zip/ttk_lingshi.zip, giữ binary bên trong nguyên vẹn. Đổi cả XML Texture filename và Element name thành ttk_...tex, copy TEX không sửa bytes.
- [ ] Định nghĩa tên tiếng Việt đúng cấp và costs thành bảng duy nhất:
```lua
return {
    tier_names = {"Hạ Phẩm Linh Thạch", "Trung Phẩm Linh Thạch", "Thượng Phẩm Linh Thạch", "Cực Phẩm Linh Thạch"},
    conversion_cost = { [2] = 100, [3] = 10, [4] = 10 },
    drop_min = 2, drop_max = 5,
    table_perish_rate = -0.2,
}
```
- [ ] Prefab factory MakeStone(tier) tạo entity Transform/AnimState/Network, MakeInventoryPhysics, bank/build xd_lingshi, PlayAnimation(tostring(tier)), MakeInventoryFloatable; SetPristine rồi master guard.
- [ ] Server add inspectable/inventoryitem/stackable/tradable, stack max TUNING.STACK_SIZE_TINYITEM, atlasname ttk_lingshiN.xml, imagename ttk_lingshiN; MakeHauntableLaunch.
- [ ] Không tag xd_lingshi và không component xd_jllingshi: dùng ttk_lingshi nếu cần nhận diện. Không thay STACK_SIZE_TINYITEM toàn cục. Cả 4 tier có cùng mẫu component chuẩn.
- [ ] Export đúng 4 prefab, ví dụ `return MakeStone(1), MakeStone(2), MakeStone(3), MakeStone(4)`; file name `ttk_lingshi` phải có trong PrefabFiles.
- [ ] Rà source paths/XML để không còn atlas trỏ file thiếu. Asset origin lưu CREDITS/report.

## Task 3 — Đăng ký recipe, strings, minimap và container
**Files:** modmain.lua, ttk_registration.lua.
**Consumes:** defs và prefab names. **Produces:** cả host/client nhận registry giống nhau.
- [ ] PrefabFiles={"ttk_hhlmz","ttk_lingshi"}; load defs/registration bằng tên ttk_ duy nhất.
- [ ] Đăng ký inventory atlas cho bàn và bốn viên; minimap atlas riêng cho bàn. Assets khai báo ANIM/ATLAS/IMAGE cần thiết. Không mang mod.manifest của mod gốc vào: local source phải được tìm theo cơ chế local mod; nếu cần manifest registration, dùng ManifestManager API đã xác thực thay vì copy manifest cũ.
- [ ] STRINGS.NAMES, RECIPE_DESC, CHARACTERS.GENERIC.DESCRIBE đầy đủ, không overwrite STRINGS table sẵn có.
- [ ] Ba recipe nâng phẩm:
```lua
for tier = 2, 4 do
    local name = "ttk_lingshi" .. tier
    env.AddRecipe2(name,
        { G.Ingredient("ttk_lingshi" .. (tier - 1), defs.conversion_cost[tier]) },
        G.TECH.SCIENCE_ONE,
        { atlas = "images/inventoryimages/" .. name .. ".xml", image = name .. ".tex", numtogive = 1 },
        { "REFINE" })
end
```
`G=GLOBAL` truyền từ env hoặc game global; đối chiếu Ingredient constructor nếu cần atlas arg. Không recipe tier1; không số lượng output100.
- [ ] Recipe bàn đúng 3/5/10, SCIENCE_ONE, placer ttk_hhlmz_placer, min_spacing4, filters STRUCTURES/DECOR/COOKING.
- [ ] Dùng require("containers").params.ttk_hhlmz, không override widgetsetup:
```lua
local p = {widget={slotpos={}, animbank="ui_backpack_2x4", animbuild="ui_backpack_2x4",
    pos=G.Vector3(275,0,0), side_align_tip=100}, type="chest"}
for y=0,3 do
    table.insert(p.widget.slotpos, G.Vector3(-162,-75*y+114,0))
    table.insert(p.widget.slotpos, G.Vector3(-87,-75*y+114,0))
end
p.itemtestfn=function(container,item)
    return item ~= nil and item:HasTag("preparedfood") and not container.inst:HasTag("burnt")
end
```
- [ ] MAXITEMSLOTS=math.max(existing,8), không giảm của Solo. Registry chạy trước `if not ismastersim return` trong bất cứ đường gọi nào.
- [ ] Rà builder/inventory vanilla để xác nhận ingredient100 dùng nhiều stack; không viết hook inventory chỉ để hỗ trợ100.

## Task 4 — Công trình và giữ hành vi gốc
**Files:** prefabs/ttk_hhlmz.lua, ttk_chestupgrade.lua.
**Consumes:** container registry. **Produces:** bàn standalone đầy đủ save/load.
- [ ] Dựa prefab đã giải để tạo entity network đúng thứ tự, giữ bank/build xd_hhlmz và idle, obstacle radius1.5, minimap ttk_hhlmz.tex, structure tag, snow.
- [ ] Chỉ master add inspectable/container/lootdropper/workable/preserver. WidgetSetup("ttk_hhlmz"), skip sounds chuẩn như gốc; callbacks phát chest_open/chest_close nhưng không PlayAnimation open/closed của chest.
- [ ] Work HAMMER, workleft3, drop contents và loot trước remove, collapse_small metal như nguồn. FX có guard nếu spawn thất bại. Preserver -0.2; không add burnable.
- [ ] Hiển thị food_1..food_8 qua itemget/itemlose. Loại dữ liệu thiếu slot/item; ClearOverrideSymbol slot hợp lệ; get recipe an toàn cho đồ gia vị và đồ mod. Nếu không biết build, bỏ trang trí cho món đó nhưng vẫn cất/lấy được; không throw nil recipe.spice.
- [ ] Giữ hỗ trợ item.Get_Myth_Food_Table nếu là function; dùng cooking.recipes/IsModCookingProduct đúng bản game. Tách tên base trước hậu tố _spice; không làm lookup spice sai món.
- [ ] Dựng lại trang trí sau load bằng OnLoadPostPass hoặc task0 theo thứ tự component load đã đọc; lặp container slots và clear ô rỗng. Không lưu duplicate contents ngoài component container.
- [ ] Helper upgrade cục bộ thay XD_MORESTACK: UPGRADETYPES.CHEST, onupgrade EnableInfiniteStackSize(true), clear upgradetype khi đã nâng, loot shard như source, load đọc upgradeable.numupgrades rồi áp lại.
- [ ] Xử lý overstack dùng các API vanilla DropEverything(nil,true), DropEverythingUpToMaxStacks và collapsed_treasurechest:SetChest. Chỉ dùng ngưỡng TUNING vanilla đã đọc; không loop spawn mọi viên hàng triệu lần.
- [ ] Khi giải thể, phối hợp ondeconstructstructure và no_delete_on_deconstruct theo vanilla để không mất container đang nằm trong pile. Khi phục hồi, animation idle + workleft3; không animation rebuild/closed không tồn tại.
- [ ] Kết hợp OnLoad cũ nếu helper bọc callback; luôn chuyển tiếp data,newents. Không thay global helper hoặc hàm trên tất cả rương.
- [ ] Dependencies khai báo collapse_small/chestupgrade_stacksize_fx/alterguardianhatshard/collapsed_treasurechest cần thiết; credits ghi nguồn logic chuẩn.

## Task 5 — Phân loại quái và loot thêm phía server
**Files:** ttk_enemy_rules.lua, ttk_loot.lua, modmain.lua.
**Consumes:** defs + four prefabs + Solo evidence. **Produces:** đúng một lượt 2..5 hạ/thượng mỗi cái chết đủ điều kiện.
- [ ] `Classify(inst)` trả nil trước nếu thiếu health/combat, prefab, hoặc player/playerghost/FX/structure/companion/critter/shadowminion/shadow_minion; loại follower có player leader. Đưa known shadow prefabs vào exclude table phòng tag bị đổi, gồm igris/beru/macanh/hacanh/fruitfly.
- [ ] Loại các phase chuyển tiếp alterguardian_phase1/phase2 nếu source vanilla cho thấy chúng dùng death để chuyển phase. Xem thêm các boss đặc biệt gần danh sách dùng, ghi bảng loại trừ có lý do; không hứa hỗ trợ mọi mod boss chưa đọc.
- [ ] Boss trước normal: epic OR boss OR hh_tags.boss_monster/endgameboss_monster (table guard, giá trị không nil/false). Các boss Solo không có tag cần bảng tên chính xác từ nguồn; không sử dụng ngưỡng maxhealth.
- [ ] Normal: monster OR hostile OR hh_is_dungeon_monster==true OR hh_dungeon_mob, sau exclusions. Chỉ nhãn dungeon đã xác thực, không in_solo_dungeon. Không gọi hàm Solo: mod độc lập khi Solo không có.
- [ ] `Attach(inst)` chỉ chạy khi TheWorld.ismastersim và health/combat tồn tại. Gắn listener ngay ở postinit, đánh giá Classify lúc death thay vì đóng băng tier lúc spawn. Guard `_ttk_loot_attached` để không attach đôi.
- [ ] Đối chiếu health.lua và stategraph death liên quan để lựa chọn callback không bỏ lỡ chết ngay sau spawn. Không DoTaskInTime(0) cho việc rơi đồ nếu entity có thể Remove trong frame chết.
- [ ] Khi death callback, Classify lại, server guard, chốt `_ttk_death_paid` trước spawn, count=math.random(2,5), prefab=boss and ttk_lingshi3 or ttk_lingshi1. Spawn thêm với `components.lootdropper:SpawnLootPrefab(prefab)` nếu có; không gọi DropLoot gốc hoặc thay loot table.
- [ ] Ưu tiên spawn count lần (chỉ2..5 entity), đi qua hook loot vanilla của Solo; không set stacksize sau SpawnLootPrefab vì hook autostack có thể nhận item trước đó. Nếu victim thiếu lootdropper, fallback SpawnPrefab+Transform tại vị trí chết không thay component của quái. Không import hh_monster_autostack.
- [ ] Một death chỉ phát đúng nhóm boss hoặc normal; không hai independent listeners. Không hook onremove hoặc tự động thêm loot vào cây/tường.
- [ ] Xem health.lua sự kiện respawnfromghost/onresurrect/healthdelta thực tế: chỉ reset chốt khi health trở lại >0, không reset trong cùng death dispatch. Không đoán tên event. Quái hồi sinh thật rồi chết lại nhận lượt mới. Ghi cơ chế chosen và lý do trong report.
- [ ] Save dead corpse: attach không tự spawn; chỉ death mới có loot. Nếu game load phát death từ component load, lưu marker riêng bằng callback save/load chain hoặc cơ chế phù hợp sau khi đọc source; không reset marker vô điều kiện khi attach.
- [ ] Đăng ký `AddPrefabPostInitAny(require("ttk_loot").Attach)` thông qua env. Không global lootdropper patch, không sửa Solo. Cost per entity một guard nhanh, no periodic scanning world.

## Task 6 — Tài liệu, rà soát và ZIP
**Files:** README_VI.md, CREDITS.md, CHANGELOG.md, report, ZIP.
**Consumes:** source mod complete. **Produces:** shareable local mod and honest verification statement.
- [ ] README: cách giải nén TuTienKy vào DST/mods trên từng máy; host enable server mod, cả master/caves và client cùng bản. Không đặt chỉ ZIP vào game. Không tự install vào game thật.
- [ ] Ghi đúng4cấp, 3tỉ lệ đổi, 2quy tắc rơi, công thức bàn; bàn chỉ nhận preparedfood, hồi độ tươi, nâng cấp chuẩn, không skins/tu luyện. Dùng tên nguyên liệu tiếng Việt.
- [ ] Ghi bảng boss/quái exclusions hỗ trợ, fallback nhận diện, chuyển phase, pet exclusions; enemy chết do đệ tử vẫn tính. Đừng gọi creature trung lập là quái chỉ vì combat.
- [ ] CREDITS: nguồn Tu Tiên 【登仙】 workshop3235319974, authors từ modinfo: 薪人小黄、路障僵尸、吃不吃大肉丸子; ghi adaptation độc lập và Klei API/logic rương. Không giả tác giả asset.
- [ ] Changelog0.1.0 và report: những gì giữ, thay, nguồn; danh sách source file thay đổi phải chỉ folder mới/docs.
- [ ] Rà tĩnh five Review Focus với đối chiếu source game; không tạo/chạy suite theo user. Compile Lua nếu runtime sẵn, loadstring chỉ parse không execute prefab/modmain; không pip install.
- [ ] Artifact check: every declared asset exists, XML points valid TEX, zip entries no parent traversal, no encrypted source/source mods, no .git/test-runtime/cache; root duy nhất TuTienKy, modinfo/modmain trực tiếp bên dưới.
- [ ] ZIP allowlist từ mods/TuTienKy, đừng zip mods hay mod_steam. Viết TuTienKy_v0.1.0.zip, tính SHA256/size và CRC check. Không claim kiểm thử game.
- [ ] Gửi coordinator source folder/ZIP/report/plan paths, per-task status, unresolved limitations. Coordinator đọc independent review rồi fix có giới hạn nếu cần.

## Quy trình thực thi, quyền ghi và status
- Người dùng đã yêu cầu làm mod và giao Sol; thực thi ngay sau khi đọc plan, không hỏi lại đã chốt.
- Project nằm ngoài writable roots cũ của phiên. Đọc được không đồng nghĩa ghi được: mọi ghi bị sandbox chặn phải dùng exec_command `sandbox_permissions=require_escalated`, không lách bằng tool khác. Có thể soạn ở temp rồi xin copy vào output đã định danh bằng command hẹp. Không đặt mã mới vào thư mục Steam cũ.
- Không cần worktree: toàn bộ input mod đang untracked ở bản copy người dùng, output là folder riêng; không sửa code Git tracked. Không git commit/push/PR trong task này.
- Nếu cần Python, runtime sẵn: `C:/Users/hoanc/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe`. Bản copy `.fasttravel-test-runtime` có thể chứa lupa; chỉ dùng nếu import được, không cài lại.
- Progress report ghi từng task complete/in progress và các Ruling. Trước khi nói done, mở ZIP/read source cuối và ghi điều gì đã/ chưa xác nhận.
- Chỉ dừng nếu thiếu nguồn, quyền ghi không được cấp, hoặc mọi giải pháp phụ thuộc phỏng đoán. Tự xử lý lựa chọn reversible theo spec.

## Tự rà plan trước handoff
- Bốn prefab/cấp được Task2 cung cấp; Task3 recipe và Task5 drop dùng đúng tên.
- 100/10/10 và 2..5/2..5 hiện thống nhất; tier1 không có recipe.
- Client registry tách khỏi server-only loot; master không nhầm master shard.
- Không phụ thuộc khai báo GLOBAL của Tu Tiên; helper upgrade và food display đều scoped.
- Quy định bỏ kiểm thử được giữ; cú pháp/ZIP không được quảng cáo như runtime test.
- Phạm vi chỉ bản copy, output mới và docs; không chạm Steam đang hoạt động.

## Kết quả triển khai và điều chỉnh đã áp dụng

- Sáu task đã tạo đủ mã, tài nguyên, tài liệu và ZIP; compile cú pháp chưa hoàn tất, game/test suite không chạy theo yêu cầu. Chi tiết bằng chứng và rulings ở `../reports/2026-09-19-tu-tien-ky-progress.md`.
- Task5 thay Attach(inst)/postinit-any bằng Install(world) và một listener entity_death phía master; lọc cause=file_load, dùng pre_health_setval để mở lại chốt khi hồi sinh. Lý do: world event có trước việc listener khác xóa quái.
- Nhận diện riêng killerbee/mosquito theo source vanilla; giữ boss-priority và loại đệ tử.
- Task4 giữ lại bàn/đồ khi collapse không tạo được hoặc vị trí không passable; không thả số lượng overstack không giới hạn.
- Modmain dùng env có sẵn; module thông thường dùng globals game, và giới hạn slot đặt ở containers.MAXITEMSLOTS.
- Output: `mods/TuTienKy`; ZIP: `mods/dist/TuTienKy_v0.1.0.zip`.
